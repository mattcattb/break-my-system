{
  "name": "@break-my-system/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/public.ts",
    "./public": "./src/public.ts"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun run --filter @break-my-system/wad-client build && make -C ../../systems/wad-filesystem && sh -c 'WAD_LISTEN_PORT=27374 WAD_DATA_DIR=/tmp/break-my-system/wads ../../systems/wad-filesystem/wadsrv-bin & pid=$!; trap \"kill $pid 2>/dev/null || true\" EXIT; sleep 0.1; WAD_URL=wad://127.0.0.1:27374 bun test'",
    "start": "bun run dist/index.js",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@break-my-system/minesweeper-client": "workspace:*",
    "@break-my-system/wad-client": "workspace:*",
    "@hono/zod-validator": "^0.4.0",
    "better-auth": "^1.2.0",
    "drizzle-orm": "^0.38.0",
    "hono": "^4.6.0",
    "hono-pino": "^0.10.3",
    "pino": "^10.3.0",
    "postgres": "^3.4.0",
    "redis": "^5.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "drizzle-kit": "^0.30.0",
    "pino-pretty": "^13.1.3"
  }
}
